name: data

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  domain:
    path: ../domain
  core:
    path: ../core

  # di
  get_it:

  # networking
  dio: 4.0.6

  #json
  json_annotation: 4.6.0

  # database
  shared_preferences: 2.0.15

dev_dependencies:
  build_runner:
  json_serializable: 6.3.1
